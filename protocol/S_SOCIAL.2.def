int64 gameId
int32 socialId
int32 type # 0: player social, 2: vehicle social
byte  superSocial # The client will wait for anim end if super socializing, super emotes will cancel others as well. No idea what it actually is but that's what i noticed.
